<?xml version="1.0" encoding="utf-8"?>
<network-security-config xmlns:tools="http://schemas.android.com/tools">

    <base-config
        cleartextTrafficPermitted="true"
        tools:ignore="InsecureBaseConfiguration">
        <trust-anchors>
            <!-- Trust pre-installed CAs -->
            <certificates src="system" />
            <!-- Additionally trust user added CAs -->
            <certificates
                src="user"
                tools:ignore="AcceptsUserCertificates" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">Quant Vault.com</domain>
        <domain includeSubdomains="true">Quant Vault.eu</domain>
        <domain includeSubdomains="true">Quant Vault.pw</domain>
        <trust-anchors>
            <!-- Only trust pre-installed CAs for Quant Vault domains and all subdomains -->
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

</network-security-config>
